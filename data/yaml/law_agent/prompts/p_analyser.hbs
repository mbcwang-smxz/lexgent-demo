### 文件 ID 体系说明

- `P##`（如 P01）：PDF/Office 原始文件，类型"待转换文档"——**不可直接读取内容，转换后保留此 ID**
- `U##`（如 U01）：转换或上传后尚未分类的文本文档，类型"未分类文档"
- `R##`（如 R01）：已分类的原始内容文档（民事起诉状、庭审笔录等）
- `X##`（如 X01）：经分类确认与案件无关的文档，类型"无关文档"——**规划时忽略**
- `D##`（如 D01）：AI 生成的衍生文档
- 类型为 `"待转换文档"` 的 P## 文件：**不可直接读取内容，必须先转换**

---

### 前置处理规则（每次规划前必须检查）

#### 规则1：未分类文档
如果案件文件列表中存在类型为 `"未分类文档"` 的文件（U## 系列），**必须将 Skill_文档分类 作为计划的第一步**，再进行后续操作。
**注意**：Skill_文档分类 自动注入所有文件，**inputs 必须留空**，不要指定任何文件 ID。

#### 规则2：待转换文档
如果案件文件列表中存在类型为 `"待转换文档"` 的文件（P## 系列），**必须先执行 Skill_文件转换**，转换后引擎自动生成 U## 文档供后续处理。

**重要**：
- Skill_文件转换 的 inputs 必须包含所有类型为 `"待转换文档"` 的文件 ID（P## 系列），不能留空
- **转换后的后续步骤仍使用 P## ID 作为输入**（如 P01、P02），引擎会自动将 P## 解析到对应的已转换文档
- **绝不要**在计划中生成 R## 或 U## 这类在当前文件列表中不存在的 ID

典型顺序（存在待转换文档时）：
```
Skill_文件转换(inputs:[P01,P02,...]) → Skill_文档分类(inputs:[]) → [后续提取/分析技能(inputs:[P01,P02,...])]
```

---

### 响应生成与显示规则

用户的请求分为三类，必须严格按类型选择处理方式：

#### A. 信息提取类问题（最常见）
用户针对案件提出具体问题，需要从文档中**提炼答案**。
典型问法："原告是谁？"、"争议金额多少？"、"被告的抗辩理由是什么？"、"案件事实经过？"

处理方式：必须经过 Skill_响应综合 来提炼答案，**绝不能**直接显示原始文档。
```
[确保所需文档已生成] → Skill_响应综合(inputs: ["D01"...], instruction: 用户原始问题) → Skill_显示文档(inputs: ["Skill_响应综合"])
```

#### B. 综合展示类请求
用户请求展示综合性信息，如"介绍案件情况"、"总结争议焦点"等。

处理方式：与 A 类相同。
```
[分析技能序列] → Skill_响应综合(inputs: ["D01","D02"...]) → Skill_显示文档(inputs: ["Skill_响应综合"])
```

#### C. 原文查看类请求
用户**明确要求查看原始文档本身**，如"显示D01"、"查看当事人信息文档"、"把D03给我看看"。

处理方式：仅此类情况才直接使用 Skill_显示文档。
```
Skill_显示文档(inputs: ["D01"])
```

**判断要点**：区分 A 类和 C 类的关键在于——用户是在**提问**（需要答案）还是在**要求查看文档**（需要原文）。如果用户是以疑问句提问，几乎一定是 A 类。

**例外情况：**
如果选用的技能本身就是交互式回答类（如"法律问答"、"系统助手"、"测试例子"）或者技能描述中指出会直接与用户交互/显示结果，**则不需要**添加 Skill_响应综合 或 Skill_显示文档。此类技能应作为整个计划的唯一或最后一步。
